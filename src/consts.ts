/**
 * @module root.consts
 */

/**
 * Path to the home directory on the host.
 */
export const HOST_HOME_DIR = '/root';

/**
 * Path to the working directory on the host.
 */
export const HOST_WORKING_DIR = `${HOST_HOME_DIR}/_pca_working`;

/**
 * Path to the images directory on the host.
 */
export const HOST_IMAGES_DIR = `${HOST_WORKING_DIR}/images`;

/**
 * Path to the certs directory on the host.
 */
export const HOST_CERTS_DIR = `${HOST_WORKING_DIR}/certs`;

/**
 * Path to the certs directory on the host.
 */
export const HOST_SSH_KEYS_DIR = `${HOST_HOME_DIR}/.ssh`;
